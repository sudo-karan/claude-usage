package com.claudeusage.app.ui.theme

import androidx.compose.ui.graphics.Color

// Brand
val Coral = Color(0xFFD97757)
val CoralBright = Color(0xFFE0875C)

// Meter accents — deliberately distinct hues so adjacent bars never blend.
val AccentSession = Color(0xFFE0875C) // warm coral
val AccentWeekly = Color(0xFF7E8AE6)  // periwinkle
val AccentSonnet = Color(0xFF5BB39B)  // teal
val AccentExtra = Color(0xFFE3B341)   // amber (Opus / additional windows)

// Warm-neutral "ink" surfaces (dark-first, premium, matte).
val InkBackground = Color(0xFF14110D)
val InkSurface = Color(0xFF1E1A14)
val InkSurfaceVariant = Color(0xFF272218)
val InkSurfaceHigh = Color(0xFF2F2819)
val OnInk = Color(0xFFEFE9DD)
val OnInkMuted = Color(0xFFA89F8E)
val OutlineSubtle = Color(0x1FFFFFFF)
val TrackNeutral = Color(0xFF3A3225)

// Status
val SuccessGreen = Color(0xFF5BB39B)
val WarnAmber = Color(0xFFE3B341)
val DangerRed = Color(0xFFE0685C)
